# Node modules directory
node_modules/

# Environment variables file
.env

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS-specific files
.DS_Store
Thumbs.db

# IDE and Editor-specific files
.vscode/
.idea/

# Knex migrations and seeds compiled files (if any)
migrations/*.cjs
seeds/*.cjs

# Optional: Ignore database files if using SQLite (you can add/remove as needed)
*.sqlite
*.sqlite3

# Optional: Ignore coverage directory generated by testing tools like Jest
coverage/

# Optional: Ignore package-lock.json or yarn.lock if not using lockfiles for consistency
# Uncomment if you prefer not to track lock files (recommended to track for dependency consistency)
# package-lock.json
# yarn.lock
